If a challenge's provided code produces the target output without
changes, lock all code lines.

Track all interface concepts.

Track all language concepts.

Minimize the number of concepts introduced at once.

Show challengs that use language concepts in provided code at least
twice before expecting learners to produce code using the concept
themselves.
